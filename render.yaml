services:
  - type: web
    name: brain-tumor
    runtime: python
    plan: free

    buildCommand: |
      python3.11 -m pip install --upgrade pip
      pip install -r requirements.txt

    startCommand: gunicorn app:app

    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9